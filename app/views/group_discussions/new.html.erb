<h1>Neue Gruppendiskussion</h1>

<div class="input">
	<%= form_for(@group_discussion, :html => { :multipart => true }) do |f| %>
		
<% if @group_discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_discussion.errors.count, "error") %> prohibited this question from being saved:</h2>

      <ul>
      <% @group_discussion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	 	<div class="field">
			<%= f.label :name, "Name" %><br>
			<%= f.text_field :name %>
		</div>
		<div class="field">
			<%= f.label :description, "Bezeichnung" %><br>
			<%= f.text_field :description %>
			</div>		
		<div class="field">
			<%= f.label :appointment, "Datum" %><br>
			<%= f.text_field :appointment %>
		</div>
			<%= f.submit "Neue Gruppendiskussion", class: "btn btn-large btn-primary" %>
	<% end %>
</div>
