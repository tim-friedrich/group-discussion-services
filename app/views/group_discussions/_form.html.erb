<%= form_for(@group_discussion) do |f| %>
  <% if @group_discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_discussion.errors.count, "error") %> prohibited this group_discussion from being saved:</h2>

      <ul>
      <% @group_discussion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
