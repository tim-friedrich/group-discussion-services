<% fmt = defined?(fmt) && fmt %>
<% tooltips = fmt == 'mini' ? false : true %>
<% duration = fmt == 'mini' ? 0 : nil # nil: default %>
<%= generate_interactive_chart type: 'user', domid: chart_id, data: data, fmt: fmt, js: { tooltips: tooltips, duration: duration } %>
