<h1>Einstellungen</h1>

<%= form_for(@user, :html => { :multipart => true, class: "form-horizontal", role: "form" }) do |f| %>
	
	<%= render 'shared/error_messages' %>

 	<%= render 'form' %>
<% end %>